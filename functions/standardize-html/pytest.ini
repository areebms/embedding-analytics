[pytest]
testpaths = tests
pythonpath = src . ../../
addopts =
    --cov=book_records
    --cov=llm_request
    --cov=llm_response
    --cov=app
    --cov=constants
    --cov-report=term-missing
    --cov-fail-under=85
